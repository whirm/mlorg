mlorg's dependancy:

- Batteries (≥ 2.0.0)
- Cmdliner

To compile mlorg:

: ocaml setup.ml -configure --prefix=prefix
: make 

To install mlorg:
- sudo make install PREFIX=prefix

You can build the doc via =make doc=.
